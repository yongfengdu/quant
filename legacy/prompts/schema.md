## 输出格式

只输出 JSON（放在 ```json 代码块中）：

```json
{
  "id": "H{NEXT_ID}",
  "name": "简短名称（≤30字）",
  "hypothesis": "市场逻辑（≤200字）。为什么有效？",
  "expected_gain": "预期提升",
  "risks": "主要风险",
  "changes": [
    "改动1: 具体做法",
    "改动2: 具体做法"
  ]
}
```

⚠️ 必须是合法的 JSON，不要加多余文字。
