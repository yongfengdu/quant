# 信号发现与生命周期系统 — 实施计划

> 版本: v1 (2026-08-30)
> 状态: 执行中
> 更新规则: 每完成一项任务更新本文件；发现新漏洞/需求时评估后动态调整

---

## 一、目标与核心认知

**目标**：建成自进化系统，在信号衰减前持续发现、验证、上线、退役信号。

**核心认知**（沉淀自整轮调研）：
1. 零和的是**信号**，不是框架；系统使命 = 持续换信号
2. 目标 = 最大化 **IC**（超额收益的截面秩相关），不是准确率、不是 edge（已实测 edge 不稳）
3. 池子真实信号 = **截面反转**（20 日尺度）；2 天方向是低 SNR 死路
4. 多重比较是元问题（60 候选/月 → 2 假通过），必须控制
5. 反转信号阶段性（2024-25 强、2026-H1 塌缩）→ 必须有衰减监控
6. A股约束（T+1、无做空、成本）→ 长周期、多头、月度调仓

## 二、已定决策

| # | 决策 | 结论 |
|---|------|------|
| 1 | 数据源 | AkShare（不复权 + 分红事件）为主，腾讯兜底 |
| 2 | 退市股 | 不拉，接受幸存者偏差局限 |
| 3 | regime | 简单方案：信号须全 regime 鲁棒（硬门槛），regime 仅诊断 |
| 4 | 存储 | Parquet + JSON（项目现无 DB，不引入新引擎） |
| 5 | 数据历史 | 尽量拉长（2018+） |

## 三、架构

```
① 信号表示(因子库) → ② 发现(系统网格+LLM) → ③ 验证(5闸门) → ④ 组合(截面多头)
                                              ↓
                                       ⑤ 生命周期(注册/衰减/退役) ← ⑥ 衰减监控
```

## 四、验证协议（5 闸门）

1. **正确性**：无前视、NaN 占比、非平凡、代码 AST 校验
2. **IC 显著性**：对**超额收益**算 IC + t-stat，多重比较控制（系统网格 Bonferroni |t|>3.8，LLM 用 FDR）
3. **稳定性**：滚 IC 符号一致性 >60% + 近期 60 日 IC 仍显著（内置衰减拦截）
4. **Walk-forward**：发现窗口选、验证窗口验（时间切分）
5. **Regime 硬门槛**：全 regime 显著才准入
- 额外：换手率/成本调整后 IC 检查

## 五、任务拆解与进度

| 阶段 | 任务 | 验收标准 | 状态 |
|------|------|---------|------|
| P0 数据 | T0.1 验证 AkShare 不复权+分红 | 不复权与腾讯一致；分红接口可用 | ⏳ |
| | T0.2 建 adj_factor 表 | 可重建任意 as-of 价格 | ⏳ |
| | T0.3 拉长历史(2018+) | 历史覆盖 2018+ | ⏳ |
| P0.5 存储 | T0.4 交易日历 | 单一真相源 | ⏳ |
| | T0.5 审计 JSON | append-only 审计 | ⏳ |
| | T0.6 ingest 完整性校验 | 重复/丢失/脏数据有检测 | ⏳ |
| P1 验证引擎 | T1.1 IC 引擎(超额) | 对超额收益算 IC | ⏳ |
| | T1.2 原语因子库 | ~30 原语 × 3 变换 | ⏳ |
| | T1.3 五闸门 | 20日反转过闸、噪声被拒 | ⏳ |
| | T1.4 成本/换手检查 | 成本调整后 IC | ⏳ |
| P2 元验证 | T2.1 合成注入 | 精度>90% | ⏳ |
| | T2.2 已知信号回放 | 20日反转 sanity | ⏳ |
| | T2.3 衰减标定 | 退役阈值反推 | ⏳ |
| P3 生命周期 | T3.1 信号注册表 | JSON 注册 | ⏳ |
| | T3.2 滚 IC 监控 | 衰减可测 | ⏳ |
| | T3.3 自动退役+补位 | 合成衰减及时退役 | ⏳ |
| P4 LLM | T4.1 假设 prompt | 因子代码+组合式 | ⏳ |
| | T4.2 快反馈+全量验证 | LLM 走确定性验证 | ⏳ |
| P5 组合 | T5.1 信号合成 | IC 加权 | ⏳ |
| | T5.2 截面多头+月度调仓 | 组合回测优于基准 | ⏳ |
| P6 自动化 | T6.1 cron | 无人值守 | ⏳ |
| | T6.2 超参 holdout | 防系统级过拟合 | ⏳ |

## 六、测试策略

单元（因子/IC/日历/ingest/复权）、集成（端到端）、属性（随机不崩）、黄金回归（20日反转 IC 快照）、合成注入（元验证）、回放（重放历史）。

## 七、工作流程

1. 每完成一个任务 → 停下来确认
2. 发现新漏洞/需求 → 评估 → 动态调整计划与优先级 → 再继续
3. 每阶段结束时更新本文件

## 八、数据层设计（Parquet + JSON，无 DB）

- **Parquet**（表格数据）：daily_bars（raw 不复权 + adj_factor）、trading_calendar、universe
- **JSON**（注册表/状态/审计）：signal_registry、signal_ic、data_audit、decay_state
- **point-in-time 复权**：存不复权原始价 + adj_factor 表，`adj_price(day, as_of) = raw(day) * Π(factor with ex_date ≤ as_of)`
- **完整性**：ingest 时 Python 校验（唯一性/OHLC 合理性/完整性/连续性），交易日历为单一真相源

## 九、执行记录

### 2026-08-30 T0.1 数据源验证 ✅

**验证结果**：
1. AkShare `stock_zh_a_hist(adjust='')` 返回不复权 OHLCV（11 列，含换手/振幅），与腾讯不复权一致（600519 收盘 1730.01 两源相同）
2. AkShare `stock_history_dividend_detail` 返回分红送配（送股/转增/派息 + 除权除息日），600519 有 31 次事件（2002-2026）
3. 腾讯 fqkline 支持不复权/qfq/hfq（服务端复权），6 字段

**关键发现（数据完整性）**：
- 代理 `proxy.ims.intel.com:911` **必需**（无代理无法访问外网），但**间歇性断连** → 现有 update_data.py 无重试，是历史数据丢失的根源之一
- **数据层必须加重试 + 双源兜底**

**复权方案决策（T0.2 定案）**：
- 腾讯 hfq（后复权）是 point-in-time 正确且稳定的（新分红不回溯历史 hfq 值）
- 方案：存**不复权 raw + adj_factor(=hfq/raw)**，返回按 `adj_ret = (raw2*f2)/(raw1*f1) - 1` 计算
- 放弃"从分红事件手工重建 qfq"（现金分红税公式有 ~1.4% 口径差）

### 2026-08-30 T0.2 split_factor 复权方案 ✅（修正）

**修正**：实测腾讯 hfq/raw 逐日漂移（非阶梯函数，含现金分红再投资），不能直接当 point-in-time 因子。
改用 **送转因子**方案：
- 存不复权 raw + `split_factor`（仅处理送股/转增，从 AkShare 分红事件构建）
- `adj_close(day) = raw_close(day) × split_factor(day)`，`split_factor` 是干净阶梯函数（除权日跳变）
- 现金分红(~1-2%/次)暂不调整，后续作 total-return 增强
- 小样本验证（10只）：split_factor 0 下降、跳变数与送转事件一致 ✓

### 2026-08-30 T0.3 历史拉取 ✅（小样本）+ 全量进行中
- `data_fetch.py`：分段拉取(腾讯640行上限→2年/段) + 重试 + 双源兜底 + split_factor 构建
- `build_data.py`：批量拉取入库 `daily_bars.parquet`，支持 resume
- 小样本 20 只验证通过：41776 行、2018-01-02~2026-08-28、0 失败
- 全量 549 只拉取中（后台，~2.7h）

### 关键结论
- 代理必需但间歇断连 → 数据层已加 5 次重试 + AkShare 兜底
- 数据层文件：`data_fetch.py`（拉取）、`build_data.py`（入库）

### 2026-08-30 P0.5 数据存储与校验 ✅
- `data_store.py`：交易日历(8797日, 节假日感知) + validate_bars(唯一/OHLC/完整性/连续性) + 审计 JSONL
- 小样本 20 只校验全通过（0 重复、0 OHLC 错误、0 缺失交易日）
- next_trading_day 正确识别国庆等法定节假日

### 2026-08-30 P1 IC 引擎 + 因子库 + 五闸门 ✅
- `factors.py`：~20 原语因子（动量/反转/量能/波动/价格位置/振幅），无前视
- `signal_lab.py`：IC 引擎（横截面秩相关）+ 五闸门 validate_signal
- 黄金测试通过：mom_20 @ H=20 IC=-9.26% t=-13.1 全 5 闸门通过
- 负例通过：噪声因子(IC-0.16%)、弱因子 rev_1(IC-1.30%) 均被拒
- **全因子扫描发现更强信号**：
  - dist_ma60 @ 20: IC=-11.66% t=-16.1（最强，价格距60日均线）
  - mom_20/mom_60 @ 20: IC≈-9.2%
  - amplitude(振幅): IC≈-6.4%（新发现）
  - 全部为反转类信号，印证"长周期反转"主导

### 待办
- T0.3 全量拉取完成（后台，132/549）
- P2 元验证（合成注入 + 衰减标定）
- T1.4 成本/换手检查（已移至 P5 组合阶段）

### 2026-08-30 P2 元验证 ✅
- `meta_validate.py`：合成注入 + 衰减标定
- 合成注入结果（549股×597日）：
  - rho=0%/0.5%/1% → 全部被拒（噪声/弱信号）
  - rho=3%/5%/10% → 全部通过
  - **精度 100%、召回 100%**（真信号 = rho≥3%）
- 衰减标定：rho=10% 中途衰减信号被闸门3正确拦截（后段 IC 从 9.6% → 0.07%）
- **关键结论**：
  1. 五闸门能正确区分真/假信号，且不误杀稳定信号
  2. **有效最小 IC ≈ 3%**（<3% 被稳定性+walk-forward 闸门拦截）
   3. 多重门设计有效：单靠显著性(gate2)会放行 rho=1%(t=6.3)，但稳定性(gate3)+walk-forward(gate4)兜住了

### 2026-08-30 P3 生命周期 ✅
- `signal_registry.py`：信号注册表(JSON) + 状态机(proposed→validated→paper→active→decayed→retired) + 衰减监控
- 衰减判定：近期(60日)滚IC |t|<1.5 连续3次 → decayed→retired（防抖，不误杀）
- 测试通过：mom_20 验证→paper→active→(注入衰减)→decayed，注册表持久化正常

### 2026-08-30 Consolidation：测试套件 ✅
- 安装 pytest，写入 `tests/`：conftest(合成数据fixture) + 5 个测试文件
- **15 个测试全部通过**（2.16s）：
  - test_factors(4): 因子正确性 + 无前视
  - test_data_store(4): 日历节假日 + 完整性校验
  - test_signal_lab(3): 黄金测试(mom_20) + 噪声拒绝 + IC引擎
  - test_signal_registry(3): 生命周期流转 + 衰减退役
  - test_meta_validate(1): 合成注入区分真假
- 旧 test_strategy_pool.py 移至 legacy_tests/（测试已废弃的 infra 系统，依赖 scipy）
- pyproject.toml 加 pytest 配置

### 待办（等全量拉取完成后）
- 用 2018+ 全量数据重跑信号验证 + 系统因子扫描
- P4 LLM 假设生成、P5 组合构建
